package com.flight.reservation.constants;

public enum ErrorCode {
    BAD_REQUEST,
    NOT_FOUND,
    UNAUTHORIZED,
    FORBIDDEN,
    INTERNAL_SERVER_ERROR, 
    VALIDATION_ERROR,
    FLIGHT_RESERVATION_ERROR
}